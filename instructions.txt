#1.install packages first:
streamlit
openai
langchain
fastapi
uvicorn
requests


#2.open cmd and enter directory then we are in code enviromnent now enter command as 'streamlit run app.py' to start application..

#3. now after opening application 'Ask something (example: weather in Hyderabad) '  to get result.
